# Test boundary

Only Claude IP guard tests and redacted fixtures belong here. Never copy live proxy credentials, runtime YAML, residential IPs, or controller state into this directory. Tests must use temporary directories and must not contact or reload the production Mihomo instance.

