# Claude IP Guard tests

This directory contains guard regression tests only.

- Fixtures must stay redacted and use documentation-only addresses.
- Tests must not read or mutate the live Clash Verge runtime.
- The suite uses only Python and shell tools already provided by macOS.

